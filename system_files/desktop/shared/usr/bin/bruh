#!/bin/bash

# Check for special case "bruh moment"
if [[ "$1" == "moment" ]]; then
  exec agungos-rollback-helper rollback "$@"
  exit
fi

# Otherwise, pass all arguments to agungos-rollback-helper
exec agungos-rollback-helper "$@"
